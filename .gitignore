# Claude Code local config — never publish
.claude/

# Claude Design working context — kept locally for Design, not pushed
uploads/
screenshots/

# Originals that assets/ files were derived from. Local only: nothing on the
# site links to them, and they are several MB of duplicates of what already
# ships. Delete this line if you want them backed up to GitHub.
sources/
Loaders.html
loaders.jsx
design-canvas.jsx

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# Editors
.vscode/
.idea/
*.swp
*~

# Logs
*.log

# Node (just in case you add tooling later)
node_modules/
